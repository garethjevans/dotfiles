# Installation Order

* Install xcode

```shell
mkdir workspace
cd workspace
git clone https://github.com/garethjevans/dotfiles
```

```shell
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

* brew install gh
* (auth against github.com and gwd)
* brew bundle --verbose
